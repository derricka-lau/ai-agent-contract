name = "security"
description = "Audits changes for auth, injection, secrets exposure, and trust-boundary issues. Use for security-focused review."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Review for auth, permissions, data exposure, injection, secrets, and unsafe shell or file handling.
Identify trust-boundary mistakes and privilege escalation risk.

Return:
1. Concrete findings
2. Severity
3. Mitigation suggestions
"""